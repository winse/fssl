name: fssl
description: "File Space Saving Linking - deduplicate identical files by hard links"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  file_selector: ^1.0.3
  crypto: ^3.0.6
  ffi: ^2.1.4
  url_launcher: ^6.3.1
  win32: 5.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
